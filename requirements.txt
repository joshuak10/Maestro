# App (inference server) deps only. Python 3.14.
# librosa + numpy are pinned exactly: hybrid_cqt output shifts subtly across
# versions, which silently degrades accuracy (train/serve skew).
librosa==0.11.0
numpy==2.4.6
torch==2.13.0
scipy==1.18.0
soundfile==0.14.0
soxr==1.1.0
audioread==3.1.0

# librosa transitive deps
numba==0.66.0
llvmlite==0.48.0
decorator==5.3.1
joblib==1.5.3
lazy-loader==0.5
msgpack==1.2.1
platformdirs==4.11.0
pooch==1.9.0
scikit-learn==1.9.0
threadpoolctl==3.6.0
typing_extensions==4.16.0

# stdlib modules removed in 3.13 that audioread still imports
audioop-lts==0.2.2
standard-aifc==3.13.0
standard-chunk==3.13.0
standard-sunau==3.13.0

# torch transitive deps
filelock==3.32.0
fsspec==2026.6.0
Jinja2==3.1.6
MarkupSafe==3.0.3
networkx==3.6.1
sympy==1.14.0
mpmath==1.3.0
setuptools==83.0.0

# pooch transitive deps
requests==2.34.2
certifi==2026.7.22
charset-normalizer==3.4.9
idna==3.18
urllib3==2.7.0

# soundfile transitive deps
cffi==2.1.0
pycparser==3.0

packaging==26.2

# Phase 2 — API server
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.20   # only needed if /predict ever takes form uploads
