# Training / notebook deps. Installs the app deps too.
-r requirements.txt

pandas==3.0.5
matplotlib==3.11.1
sounddevice==0.5.5   # script.py only — never goes in the server image

# matplotlib transitive deps
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0

narwhals==2.24.0

# jupyter is not in this env's freeze — the notebook runs on a separate kernel.
# If you want it in-env: pip install jupyter, then re-freeze.
